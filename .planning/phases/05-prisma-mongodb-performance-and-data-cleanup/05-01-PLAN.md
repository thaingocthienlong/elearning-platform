# 05-01-PLAN: Watch Query Cleanup And Index Review

## Objective

Remove redundant watch-page video queries and add targeted MongoDB indexes for watch/session/access traffic.

## Requirements

- `DATA-02`: Watch page query flow avoids redundant video queries and unnecessary broad reads.
- `DATA-04`: MongoDB indexes are reviewed and updated for high-traffic session, entitlement, watch, video, admin, ticket, and security-event queries.

## Tasks

1. Refactor `src/app/watch/[videoId]/page.tsx` so sidebar video IDs come from the already-loaded `courseVideos` result.
2. Keep the existing watch page response behavior and entitlement flow.
3. Add or adjust Prisma indexes for common watch-record and access lookup shapes.
4. Add focused test coverage where practical, or document the code-level evidence if the route is not easily unit-testable.

## Verification

- `npm run typecheck`
- `npm test`

