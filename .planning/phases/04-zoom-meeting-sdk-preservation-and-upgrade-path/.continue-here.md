---
context: phase
phase: 04-zoom-meeting-sdk-preservation-and-upgrade-path
task: 1
total_tasks: 4
status: paused
last_updated: 2026-05-05T21:09:59.2164148+07:00
---

<current_state>
Phase 3 is complete and verified. The project is paused at the start of Phase 4: Zoom Meeting SDK Preservation and Upgrade Path.

The current active task is Phase 4 smart discuss. Grey Area 1/4 was presented to the user, but the user invoked `$gsd-pause-work` before answering it. Do not write `04-CONTEXT.md` until the user answers the pending Zoom decision.
</current_state>

<completed_work>

Completed Tasks:
- Phase 1: Installable Baseline, Docs, and Secret Hygiene - Done.
- Phase 2: Central Authorization and Core Security Fixes - Done, verified in commit `d1f0e2a`.
- Phase 3: Axinom Trial Setup and DRM/Encoding Validation - Done, verified in commit `fbfea75`.
- Phase 4 prep - In progress:
  - Official Zoom docs/changelog were searched.
  - Current Zoom route/page/assets were scouted:
    - `src/app/api/zoom/signature/route.ts`
    - `src/app/meeting/page.tsx`
    - `public/zoom-meeting.html`
    - `public/zoom/`
    - `public/lib/zoom/`
    - `public/zoom-client-view/`
    - `zoom-webapp/`
  - Grey Area 1/4 was presented.
</completed_work>

<remaining_work>

- Capture user answer for Phase 4 Grey Area 1/4.
- Continue Phase 4 smart discuss areas 2/4 through 4/4.
- Write `04-CONTEXT.md` only after all Phase 4 smart-discuss decisions are captured.
- Plan Phase 4 with official Zoom docs and local code context.
- Execute Phase 4 plans, then run code review, full verification, requirement updates, and transition to Phase 5.
</remaining_work>

<decisions_made>

- Phase 3 decided Axinom v1 uses standard License Service Message mode, not local proxy mode.
- Phase 3 decided local Axinom validation does not call live APIs unless `--live` is explicitly supplied.
- Phase 3 decided Axinom operational IDs and statuses belong in explicit `Video` fields, with legacy description parsing only as fallback.
- No Phase 4 decisions are captured yet because the user paused before answering Grey Area 1/4.
</decisions_made>

<blockers>

- Human action pending: answer the Phase 4 Grey Area 1/4 question.
- No technical blockers are active.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` - current phase and progress.
1. `.planning/PROJECT.md` - authoritative product intent, constraints, and decisions.
1. `.planning/ROADMAP.md` - Phase 4 goal and success criteria.
1. `.planning/REQUIREMENTS.md` - `ZOOM-*` and `TEST-04` requirement IDs.
1. `.planning/research/ARCHITECTURE.md` - existing Zoom architecture notes and risks.
1. `src/app/api/zoom/signature/route.ts` - current server-side signature generation.
1. `src/app/meeting/page.tsx` - current authenticated iframe launch flow.
1. `public/zoom-meeting.html` and `public/zoom-client-view/` - current static meeting client assets.
1. Official Zoom Meeting SDK docs used for current-source validation:
   - https://developers.zoom.us/docs/meeting-sdk/web/
   - https://developers.zoom.us/docs/meeting-sdk/auth/
   - https://developers.zoom.us/docs/meeting-sdk/web/client-view/
   - https://developers.zoom.us/docs/meeting-sdk/web/component-view/
   - https://developers.zoom.us/changelog/meeting-sdk/web/4.0.7/

## Critical Anti-Patterns (do NOT repeat these)

- Do not stage with `git add .`; the repo has many inherited untracked baseline files because git tracking was initialized during rescue.
- Do not write Phase 4 context/plans until the pending smart-discuss answer is captured.
- Do not assume Zoom docs or SDK versions from memory; verify against official Zoom docs/changelog before changing SDK behavior.
- Do not expose `ZOOM_MEETING_SDK_SECRET` or generate signatures client-side.

## Infrastructure State

- No repo-local dev server is intentionally running. Earlier Next/Jest Node processes were stopped to clear a Prisma Windows DLL lock during Phase 3.
- `npm run prisma:generate`, `npm run verify:setup`, `npm run verify:axinom`, `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and `npm run secrets:scan` passed for Phase 3.
- `npm run lint` still passes with inherited warnings. Phase 3 reduced newly introduced warnings in touched files.
- `npm run secrets:scan` still skips gitleaks-backed scanning locally because gitleaks is not installed.

<context>
Phase 4 goal: preserve current authenticated Zoom meeting flow while making SDK source-of-truth, server-side signature generation, role handling, and upgrade/smoke testing maintainable.

Current Zoom implementation observations:
- `src/app/api/zoom/signature/route.ts` generates Meeting SDK JWTs server-side with `jsrsasign`, but it currently accepts client-supplied `role` and only fetches session after signing setup. It returns the SDK key but not the secret.
- `src/app/meeting/page.tsx` requires auth via `useSession`, posts `role: 0` to `/api/zoom/signature`, then builds an iframe URL to `/zoom-meeting.html` with signature, SDK key, meeting number, passcode, identity, and watermark params.
- Multiple Zoom asset trees exist: `public/zoom`, `public/lib/zoom`, `public/zoom-client-view`, and `zoom-webapp`. Phase 4 must pick or document one maintained source of truth.

Pending Grey Area 1/4 as presented:

| # | Question | Recommended | Alternative(s) |
|---|----------|-------------|----------------|
| 1 | What should Phase 4 preserve? | Keep current authenticated `/meeting` page + iframe launch flow | Replace with direct React SDK page |
| 2 | What is the SDK source of truth? | Use installed `@zoom/meetingsdk` package as source of truth; quarantine copied sample/static SDK assets unless still required | Keep `public/zoom*` assets; keep `zoom-webapp` sample |
| 3 | How aggressive should the upgrade be? | Verify installed SDK against official docs/changelog first, then upgrade only if needed and testable | Force latest immediately |
| 4 | How should host/admin roles work? | Default learners to role `0`; only admins may request host-capable role `1` | Keep client-supplied role; disable host signatures entirely |

Expected reply options:
1. Accept all
2. Change Q1
3. Change Q2
4. Change Q3
5. Change Q4
6. Discuss deeper
</context>

<next_action>
Start with: ask the user to answer the pending Phase 4 Grey Area 1/4 question, unless they already replied after this handoff was written. If they answer `1`, record all recommended decisions for Area 1 and continue with Phase 4 Grey Area 2/4.
</next_action>
