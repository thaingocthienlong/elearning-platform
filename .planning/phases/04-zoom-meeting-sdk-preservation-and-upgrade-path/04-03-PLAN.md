# 04-03-PLAN: Zoom SDK Source Of Truth And Upgrade Runbook

## Objective

Identify the live Zoom SDK path, quarantine duplicate/sample asset trees outside served public paths where practical, and document the official-doc-based upgrade and rollback procedure for `ZOOM-02` and `ZOOM-05`.

## Requirements

- `ZOOM-02`: Check Zoom Meeting SDK integration against official current documentation before upgrading.
- `ZOOM-05`: One maintained SDK asset/source-of-truth path and documented upgrade procedure.

## Tasks

1. Document the current live path: `/zoom-meeting.html` uses Zoom 5.0.4 CDN JS plus local `/lib/zoom/css/*`.
2. Record that npm reports `@zoom/meetingsdk` latest as `6.0.0` on 2026-05-06, while the app currently pins `^5.0.4`.
3. Quarantine unserved sample app directories outside `public/` when they are not referenced by the preserved flow.
4. Keep local `/public/lib/zoom` assets until a real staging smoke proves they can be replaced or upgraded.
5. Write `docs/zoom-meeting-sdk-runbook.md` with setup, source of truth, official docs checked, upgrade steps, smoke test, and rollback.

## Files

- `docs/zoom-meeting-sdk-runbook.md`
- `public/zoom-client-view/`
- `zoom-webapp/`

## Verification

- `rg -n "zoom-client-view|zoom-webapp|source.zoom.us/5.0.4|@zoom/meetingsdk|Zoom Meeting SDK" docs public src package.json`

