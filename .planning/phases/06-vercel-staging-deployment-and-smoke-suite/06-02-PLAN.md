# 06-02 Plan: Callback, Origin, and Env Matrix Staging Contract

## Goal

Make staging callback and origin setup explicit across Google OAuth, Axinom webhooks, Zoom, Azure Blob, Cloudflare R2, Vercel domains, and public player URLs.

## Requirements

- STAGE-02
- STAGE-03
- STAGE-06

## Scope

- Extend `docs/env-matrix.md` with staging host notes and validation commands.
- Add callback/origin sections to the Vercel staging runbook or a dedicated doc.
- Cover:
  - `NEXTAUTH_URL` and Google OAuth redirect URI.
  - Axinom webhook URL and signature secret handling.
  - Zoom Meeting SDK allowed SDK/domain configuration.
  - Azure Blob CORS and R2/public asset origin configuration.
  - Sentry environment separation and redaction expectations.
- Keep examples placeholder-only.

## Verification

- Documentation tests assert callback/origin coverage.
- `npm run verify:services` confirms service groups remain parseable.

## Status

Planned.
