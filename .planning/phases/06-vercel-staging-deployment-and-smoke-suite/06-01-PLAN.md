# 06-01 Plan: Vercel Staging Runbook

## Goal

Create a maintainer-facing Vercel staging runbook that explains deployment shape, provider prerequisites, local gates, Vercel environment handling, deployment commands, serverless limits, and acceptance boundaries.

## Requirements

- STAGE-01
- STAGE-04
- STAGE-06
- STAGE-07

## Scope

- Add `docs/vercel-staging-runbook.md`.
- Reference official Vercel docs for environments, environment variables, and function duration/limits.
- Include command sequence for `npm ci`, Prisma generation, setup verification, service/env verification, Axinom validation, lint, typecheck, tests, build, and secret scan.
- Document that Vercel env changes apply only to new deployments.
- Document that `src/app/api/video/process/route.ts` uses long request duration and should be treated as an encoding trigger/orchestrator path, not a guaranteed production encoding worker.
- Include logging/Sentry guidance with no secret values.

## Verification

- Run documentation/unit tests added in this phase.
- Run `npm run verify:setup`.

## Status

Planned.
