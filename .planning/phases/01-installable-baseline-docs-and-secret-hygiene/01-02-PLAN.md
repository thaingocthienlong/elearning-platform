---
phase: 01-installable-baseline-docs-and-secret-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-03
files_modified:
  - .nvmrc
  - scripts/verify-setup.ts
  - scripts/verify-services.ts
  - docs/setup.md
  - docs/verification.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
must_haves:
  truths:
    - "Maintainer can install dependencies from a clean checkout using documented Node and npm versions."
    - "Maintainer can generate Prisma client and prepare the MongoDB-backed schema using documented commands."
    - "Maintainer can start the local development server using the placeholder-safe env docs created from the env matrix."
  artifacts:
    - path: ".nvmrc"
      provides: "Node version pin"
      contains: "20"
    - path: "scripts/verify-setup.ts"
      provides: "Local bootstrap prerequisite checker"
      contains: "20.9.0"
    - path: "scripts/verify-services.ts"
      provides: "Default-skip and strict-fail service env checker derived from docs/env-matrix.md"
      contains: "docs/env-matrix.md"
    - path: "docs/setup.md"
      provides: "Detailed maintainer setup runbook"
      contains: "Prisma MongoDB"
  key_links:
    - from: "package.json"
      to: "scripts/verify-setup.ts"
      via: "verify:setup script"
      pattern: "\"verify:setup\": \"tsx scripts/verify-setup.ts\""
    - from: "docs/setup.md"
      to: "prisma/schema.prisma"
      via: "MongoDB datasource documentation"
      pattern: "prisma db push"
    - from: "scripts/verify-services.ts"
      to: "docs/env-matrix.md"
      via: "service variable contract parser"
      pattern: "docs/env-matrix.md"
    - from: "docs/setup.md"
      to: ".env.example"
      via: "local dev startup instructions"
      pattern: "npm run dev"
---

<objective>
Make clean-checkout bootstrap, Prisma MongoDB setup, and local dev startup explicit and verifiable.

Purpose: SETUP-01, SETUP-02, and SETUP-03 require maintainers to know the Node/npm baseline, install dependencies, generate Prisma client, prepare the MongoDB schema, and start local development using placeholder-safe env docs without following stale PostgreSQL instructions. This implements D-01 through D-03 and consumes the env source of truth from D-04 without modifying `prisma/schema.prisma`.
Output: Node pin, setup verification script, optional service verification script, setup runbook, and verification command guide.
</objective>

<execution_context>
@C:/Users/Vien Phuong Nam/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Vien Phuong Nam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-CONTEXT.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-RESEARCH.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-VALIDATION.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-PATTERNS.md
@package.json
@prisma/schema.prisma
@.env.example
@docs/env-matrix.md
@scripts/verify-auth-sync.ts
@scripts/verify-axinom-setup.ts
@scripts/verify-azure-storage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add setup and service verification scripts</name>
  <files>.nvmrc, scripts/verify-setup.ts, scripts/verify-services.ts</files>
  <read_first>
    - package.json
    - prisma/schema.prisma
    - scripts/verify-auth-sync.ts
    - scripts/verify-axinom-setup.ts
    - scripts/verify-azure-storage.ts
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-CONTEXT.md
  </read_first>
  <behavior>
    - `npm run verify:setup` checks Node `>=20.9.0`, npm availability, `package-lock.json`, `package.json`, `prisma/schema.prisma`, and required root scripts.
    - `npm run verify:services` skips missing external credentials with clear messages by default per D-02.
    - `npm run verify:services:strict` fails when service groups are missing required env vars per D-03.
    - Service required-variable groups are derived from `docs/env-matrix.md` because D-04 makes the matrix the source of truth.
  </behavior>
  <action>Create `.nvmrc` with `20.11.1`. Create `scripts/verify-setup.ts` using Node built-ins only (`node:fs`, `node:child_process`, `node:process`) to check `process.versions.node >= 20.9.0`, `npm --version`, presence of `package-lock.json`, `package.json`, `prisma/schema.prisma`, `.env.example`, `docs/env-matrix.md`, and package scripts from Plan 01. It must print only command/status names, never env values. Create `scripts/verify-services.ts` using dotenv loading for `.env` then `.env.local`. It must parse `docs/env-matrix.md` table rows as the source of truth per D-04, grouping rows by `Service` and selecting variables whose `Local` or `Staging` column is `required`; treat `--strict` or `CI=true` as strict; in non-strict mode print `SKIP <service>: missing VAR1, VAR2`; in strict mode set nonzero exit if any required group vars are missing. Include a guard that fails if required service groups from D-05 are absent from the matrix. Do not maintain a separate hardcoded service variable list beyond the required service group names, and do not call external service APIs in Phase 1.</action>
  <verify>
    <automated>npm run verify:setup && npm run verify:services</automated>
  </verify>
  <acceptance_criteria>
    - `Get-Content .nvmrc` is exactly `20.11.1`.
    - `Select-String -Path scripts/verify-setup.ts -Pattern "20.9.0" -Quiet` returns success.
    - `Select-String -Path scripts/verify-services.ts -Pattern "--strict" -Quiet` returns success.
    - `Select-String -Path scripts/verify-services.ts -Pattern "docs/env-matrix.md" -Quiet` returns success.
    - `Select-String -Path scripts/verify-services.ts -Pattern "SKIP" -Quiet` returns success.
    - `npm run verify:setup` exits 0 on a correctly installed checkout.
  </acceptance_criteria>
  <done>Clean-checkout and optional-service verification scripts exist with local skip and strict fail behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Document install and Prisma MongoDB setup</name>
  <files>docs/setup.md, docs/verification.md</files>
  <read_first>
    - README.md
    - package.json
    - prisma/schema.prisma
    - docs/architecture/system_overview.md
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-RESEARCH.md
  </read_first>
  <action>Create `docs/setup.md` with sections: `Quick Path`, `Prerequisites`, `Install From Clean Checkout`, `Environment File`, `Prisma MongoDB Setup`, `Start Local Development`, `Optional Service Checks`, and `Troubleshooting`. It must state Node `>=20.9.0`, npm with `package-lock.json`, `npm install`, copy `.env.example` to `.env.local`, use `docs/env-matrix.md` as the source of truth for required local variables per D-04, `npm run prisma:generate`, `npm run db:push`, and `npm run dev`. It must state `DATABASE_URL` is a MongoDB connection string for `provider = "mongodb"` and must not mention PostgreSQL as the active primary database. Create `docs/verification.md` listing exact root commands: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, `npm run verify:setup`, `npm run verify:services`, `npm run verify:services:strict`, `npm run secrets:inventory`, and `npm run secrets:scan`. Explain that service verification derives required variables from `docs/env-matrix.md`, then applies default-skip versus strict-fail behavior using those exact command names.</action>
  <verify>
    <automated>npm run verify:setup</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path docs/setup.md -Pattern "Node >=20.9.0" -Quiet` returns success.
    - `Select-String -Path docs/setup.md -Pattern "Prisma MongoDB" -Quiet` returns success.
    - `Select-String -Path docs/setup.md -Pattern "npm run db:push" -Quiet` returns success.
    - `Select-String -Path docs/setup.md -Pattern "npm run dev" -Quiet` returns success.
    - `Select-String -Path docs/setup.md -Pattern "docs/env-matrix.md" -Quiet` returns success.
    - `Select-String -Path docs/verification.md -Pattern "npm run verify:services:strict" -Quiet` returns success.
    - `Select-String -Path docs/setup.md -Pattern "PostgreSQL.*primary|Primary DB.*PostgreSQL"` returns no matches.
  </acceptance_criteria>
  <done>SETUP-01, SETUP-02, and SETUP-03 are documented through maintainer-facing commands without schema changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell -> verification scripts | Maintainer executes scripts that inspect local files and env names. |
| env files -> service verifier | Service verifier loads env variable names and presence, but must not print values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | `scripts/verify-services.ts` | mitigate | Implement `--strict` and `CI=true` fail behavior so staging gaps cannot be hidden by local optional checks. |
| T-1-04 | Information Disclosure | `scripts/verify-services.ts` | mitigate | Print missing variable names only; never print env values, masks, tokens, or connection strings. |
| T-1-06 | Repudiation | `docs/verification.md` | mitigate | Document exact commands and expected skip/fail semantics so verification results can be reproduced. |
</threat_model>

<verification>
Run `npm run verify:setup && npm run verify:services`. If placeholder env is not configured, `verify:services` should derive missing required variables from `docs/env-matrix.md`, exit 0 with SKIP messages, and `verify:services:strict` may fail until real staging credentials exist.
</verification>

<success_criteria>
Maintainers can identify Node/npm requirements, install dependencies, generate Prisma client, run `prisma db push` against MongoDB, start `npm run dev` from placeholder-safe env docs, and understand local versus strict verification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-02-SUMMARY.md`.
</output>
