---
phase: 01-installable-baseline-docs-and-secret-hygiene
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - .env.example
  - .gitignore
  - docs/env-matrix.md
  - __tests__/env/env-matrix.test.ts
autonomous: true
requirements:
  - SETUP-03
  - SETUP-04
must_haves:
  truths:
    - "Maintainer can start local dev with placeholder-safe environment documentation."
    - "Maintainer can identify each required env variable, owning service, sensitivity, and local/staging applicability."
  artifacts:
    - path: ".env.example"
      provides: "Copy/paste placeholder starter env file"
      contains: "DATABASE_URL=\"mongodb://"
    - path: "docs/env-matrix.md"
      provides: "Service-grouped env source of truth"
      contains: "| Service | Variable | Sensitivity | Local | Staging | Source | Notes |"
    - path: "__tests__/env/env-matrix.test.ts"
      provides: "Automated env docs contract"
      contains: "env-matrix.md"
  key_links:
    - from: ".env.example"
      to: "docs/env-matrix.md"
      via: "every starter env variable has a matrix row"
      pattern: "DATABASE_URL"
    - from: "docs/env-matrix.md"
      to: "Next.js public env rule"
      via: "NEXT_PUBLIC_ sensitivity documentation"
      pattern: "NEXT_PUBLIC_"
---

<objective>
Create the placeholder-safe env starter and service-grouped env matrix required for local startup and staging readiness.

Purpose: SETUP-03 and SETUP-04 implement D-04 through D-06. The env matrix is the source of truth; `.env.example` is only the practical starter file. No inherited env values may be read or copied.
Output: `.env.example`, env matrix documentation, `.gitignore` opt-in for the example file, and env contract tests.
</objective>

<execution_context>
@C:/Users/Vien Phuong Nam/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Vien Phuong Nam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-CONTEXT.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-RESEARCH.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-PATTERNS.md
@.planning/codebase/STACK.md
@jest.setup.ts
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add env matrix contract tests</name>
  <files>__tests__/env/env-matrix.test.ts</files>
  <read_first>
    - jest.setup.ts
    - .planning/codebase/STACK.md
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-CONTEXT.md
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-VALIDATION.md
  </read_first>
  <behavior>
    - Test fails if `.env.example` is missing core placeholders.
    - Test fails if `docs/env-matrix.md` lacks required service groups from D-05.
    - Test fails if server secret variables are marked public.
  </behavior>
  <action>Create `__tests__/env/env-matrix.test.ts` using Node `fs`/`path`. Assert `.env.example` contains exact names `DATABASE_URL`, `NEXTAUTH_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `AXINOM_COM_KEY_ID`, `AXINOM_COM_KEY_SECRET`, `R2_ENDPOINT`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `ZOOM_MEETING_SDK_KEY`, `ZOOM_MEETING_SDK_SECRET`, `SMTP_HOST`, `RECAPTCHA_SECRET_KEY`, `SENTRY_DSN`, `NEXT_PUBLIC_RECAPTCHA_SITE_KEY`, `NEXT_PUBLIC_ZOOM_MEETING_ID`, and `NEXT_PUBLIC_ASSET_BASE`. Assert `docs/env-matrix.md` contains service headings or rows for `Database`, `Auth`, `Redis`, `Axinom`, `Storage`, `Zoom`, `Support/Email/reCAPTCHA`, `Observability`, and `Public player/config`. Assert rows for `DATABASE_URL`, `NEXTAUTH_SECRET`, and `ZOOM_MEETING_SDK_SECRET` include `server secret`, and rows for `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` and `NEXT_PUBLIC_ZOOM_MEETING_ID` include `public`.</action>
  <verify>
    <automated>powershell -NoProfile -Command 'if (!(Test-Path "__tests__/env/env-matrix.test.ts")) { exit 1 }; foreach ($pattern in @("DATABASE_URL","NEXT_PUBLIC_ZOOM_MEETING_ID","server secret")) { if (!(Select-String -Path "__tests__/env/env-matrix.test.ts" -Pattern $pattern -Quiet)) { exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path __tests__/env/env-matrix.test.ts -Pattern "DATABASE_URL" -Quiet` returns success.
    - `Select-String -Path __tests__/env/env-matrix.test.ts -Pattern "NEXT_PUBLIC_ZOOM_MEETING_ID" -Quiet` returns success.
    - `Select-String -Path __tests__/env/env-matrix.test.ts -Pattern "server secret" -Quiet` returns success.
  </acceptance_criteria>
  <done>Env docs have a Jest contract file before docs are accepted; Task 2 runs the contract after the docs artifacts exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder env example and grouped env matrix</name>
  <files>.env.example, .gitignore, docs/env-matrix.md</files>
  <read_first>
    - .gitignore
    - jest.setup.ts
    - prisma/schema.prisma
    - .planning/codebase/STACK.md
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-RESEARCH.md
  </read_first>
  <action>Create `.env.example` with placeholder-only values and comments grouped by database, auth, Redis, Axinom, storage, Zoom, support/email/reCAPTCHA, observability, and public player/config values per D-04 and D-05. Use obvious placeholders like `replace-with-google-client-id`, `mongodb://localhost:27017/secure_video_platform`, and `https://example.invalid`; do not copy any value from local env/key files. Create `docs/env-matrix.md` with a table header exactly `| Service | Variable | Sensitivity | Local | Staging | Source | Notes |` and rows for every variable listed in Task 1 plus known service variables from `.planning/codebase/STACK.md`: `AUTH_SECRET`, `REDIS_URL`, `AZURE_STORAGE_ACCOUNT`, `AZURE_STORAGE_KEY`, `AZURE_VIDEO_INPUT_CONTAINER`, `AZURE_VIDEO_OUTPUT_CONTAINER`, `R2_BUCKET`, `R2_PREFIX`, `AXINOM_ENCODING_CLIENT_ID`, `AXINOM_ENCODING_CLIENT_SECRET`, `AXINOM_ENCODING_PROFILE_DRM`, `AXINOM_ENCODING_PROFILE_CLEAR`, `AXINOM_VIDEO_SERVICE_URL`, `AXINOM_WEBHOOK_SECRET`, `AXINOM_FAIRPLAY_CERT_URL`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`, `ADMIN_EMAIL`, `NEXT_PUBLIC_ZOOM_PASSCODE`, `NEXT_PUBLIC_AX_WV_LS_URL`, and `NEXT_PUBLIC_AX_FP_LS_URL`. Mark sensitivity using exact values `public`, `server secret`, or `operational secret`; mark local/staging using exact values `required` or `optional`. Update `.gitignore` so `.env*` remains ignored but `.env.example` is explicitly unignored with `!.env.example`.</action>
  <verify>
    <automated>npm test -- env-matrix --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path .env.example -Pattern "replace-with" -Quiet` returns success.
    - `Select-String -Path docs/env-matrix.md -Pattern "| Service | Variable | Sensitivity | Local | Staging | Source | Notes |" -SimpleMatch -Quiet` returns success.
    - `Select-String -Path docs/env-matrix.md -Pattern "NEXT_PUBLIC_.*public" -Quiet` returns success.
    - `Select-String -Path .gitignore -Pattern "^!\\.env\\.example$" -Quiet` returns success.
    - `Select-String -Path .env.example -Pattern "mosaic-service-account|BEGIN PRIVATE KEY|sk_live|prod_"` returns no matches.
  </acceptance_criteria>
  <done>SETUP-03 and SETUP-04 are covered by a safe starter env file and source-of-truth env matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/examples -> maintainer copy/paste | Maintainer may copy `.env.example` into real env files. |
| server env -> browser bundle | `NEXT_PUBLIC_` variables cross into client-side code at build time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | `.env.example` | mitigate | Use placeholders only and test that known sensitive token/key patterns are absent. |
| T-1-02 | Information Disclosure | `docs/env-matrix.md` | mitigate | Require sensitivity rows for public, server secret, and operational secret variables; document `NEXT_PUBLIC_` browser exposure. |
| T-1-07 | Tampering | `.gitignore` | mitigate | Keep `.env*` ignored while explicitly unignoring only `.env.example`. |
</threat_model>

<verification>
Run `npm test -- env-matrix --runInBand`. Inspect `.env.example` only for placeholder strings and variable names, not real inherited values.
</verification>

<success_criteria>
Maintainers can create a local env file from placeholders and use `docs/env-matrix.md` to identify each variable's owner, sensitivity, and local/staging applicability.
</success_criteria>

<output>
After completion, create `.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-03-SUMMARY.md`.
</output>
