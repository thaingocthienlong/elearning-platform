---
phase: 01-installable-baseline-docs-and-secret-hygiene
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - README.md
  - docs/secret-hygiene.md
  - scripts/inventory-sensitive-files.ts
  - scripts/scan-secrets.ts
  - __tests__/docs/readme-and-secret-hygiene.test.ts
autonomous: true
requirements:
  - SETUP-05
  - SETUP-07
must_haves:
  truths:
    - "Maintainer sees concise setup documentation that corrects PostgreSQL drift."
    - "Maintainer can inventory and scan sensitive artifacts without printing secret values."
  artifacts:
    - path: "README.md"
      provides: "Concise landing page with correct MongoDB setup links"
      contains: "Prisma MongoDB"
    - path: "docs/secret-hygiene.md"
      provides: "Sensitive artifact handling policy"
      contains: "Do not read, print, copy, move, delete, or commit secret values"
    - path: "scripts/inventory-sensitive-files.ts"
      provides: "Path-only sensitive artifact inventory"
      contains: "InventoryItem"
    - path: "scripts/scan-secrets.ts"
      provides: "Redacted secret scan wrapper"
      contains: "--redact"
  key_links:
    - from: "package.json"
      to: "scripts/inventory-sensitive-files.ts"
      via: "secrets:inventory script"
      pattern: "\"secrets:inventory\": \"tsx scripts/inventory-sensitive-files.ts\""
    - from: "README.md"
      to: "docs/setup.md"
      via: "setup docs link"
      pattern: "docs/setup.md"
    - from: "README.md"
      to: "docs/env-matrix.md"
      via: "environment docs link"
      pattern: "docs/env-matrix.md"
    - from: "README.md"
      to: "docs/verification.md"
      via: "verification docs link"
      pattern: "docs/verification.md"
---

<objective>
Correct README setup drift and add repeatable secret hygiene inventory/scanning guidance.

Purpose: SETUP-05 and SETUP-07 implement D-10 through D-14. This plan inventories paths and risk categories only; it must not move, quarantine, delete, read, print, copy, or commit inherited secret values.
Output: Concise README, secret hygiene guide, path-only inventory script, redacted scan wrapper, and docs/script tests.
</objective>

<execution_context>
@C:/Users/Vien Phuong Nam/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Vien Phuong Nam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-CONTEXT.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-RESEARCH.md
@.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@README.md
@.gitignore
@docs/setup.md
@docs/env-matrix.md
@docs/verification.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add README and secret hygiene contract tests</name>
  <files>__tests__/docs/readme-and-secret-hygiene.test.ts</files>
  <read_first>
    - README.md
    - .gitignore
    - .planning/codebase/CONCERNS.md
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-VALIDATION.md
  </read_first>
  <behavior>
    - Test fails if README still claims PostgreSQL is the primary database.
    - Test fails if README does not link detailed setup, env matrix, verification, and secret hygiene docs.
    - Test fails if secret scripts do not contain path-only/redaction safeguards.
  </behavior>
  <action>Create `__tests__/docs/readme-and-secret-hygiene.test.ts` using Node `fs`/`path`. Assert `README.md` contains `Prisma MongoDB`, `docs/setup.md`, `docs/env-matrix.md`, `docs/verification.md`, and `docs/secret-hygiene.md`. Assert `README.md` does not match `/PostgreSQL\\s*\\(Primary DB\\)|Postgres \\+ Mongo|DATABASE_URL\\s*\\(PostgreSQL\\)/i`. Assert `docs/secret-hygiene.md` contains `Do not read, print, copy, move, delete, or commit secret values`. Assert `scripts/inventory-sensitive-files.ts` contains `InventoryItem` and does not contain `readFileSync`. Assert `scripts/scan-secrets.ts` contains `--redact`.</action>
  <verify>
    <automated>powershell -NoProfile -Command 'if (!(Test-Path "__tests__/docs/readme-and-secret-hygiene.test.ts")) { exit 1 }; foreach ($pattern in @("PostgreSQL","readFileSync","--redact")) { if (!(Select-String -Path "__tests__/docs/readme-and-secret-hygiene.test.ts" -Pattern $pattern -Quiet)) { exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path __tests__/docs/readme-and-secret-hygiene.test.ts -Pattern "PostgreSQL" -Quiet` returns success.
    - `Select-String -Path __tests__/docs/readme-and-secret-hygiene.test.ts -Pattern "readFileSync" -Quiet` returns success.
    - `Select-String -Path __tests__/docs/readme-and-secret-hygiene.test.ts -Pattern "--redact" -Quiet` returns success.
  </acceptance_criteria>
  <done>README drift and secret hygiene behavior have a contract test file; Tasks 2 and 3 run the contract after the implementation artifacts exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement path-only inventory and redacted scan scripts</name>
  <files>scripts/inventory-sensitive-files.ts, scripts/scan-secrets.ts, docs/secret-hygiene.md</files>
  <read_first>
    - .gitignore
    - .planning/codebase/CONCERNS.md
    - package.json
    - .planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-CONTEXT.md
  </read_first>
  <action>Create `scripts/inventory-sensitive-files.ts` using `node:fs`, `node:path`, and `node:child_process` only. It must define `type InventoryItem` with `path`, `category`, `trackedByGit`, `ignoredByGit`, `sizeBytes`, and `action`. It must scan candidate paths/patterns named in `.planning/codebase/CONCERNS.md` and `.gitignore`, but use metadata only: `fs.statSync`, `git ls-files --error-unmatch`, and `git check-ignore`. It must not call `readFileSync` or print file contents. Create `scripts/scan-secrets.ts` as a wrapper that checks whether `gitleaks` is available, runs `gitleaks detect --source . --redact --no-banner` when available, and in `--strict` or `CI=true` fails if gitleaks is missing; local default prints installation guidance and exits 0 if gitleaks is unavailable. Create `docs/secret-hygiene.md` documenting inherited artifact categories, placeholder-only examples, path-only inventory, redacted scanning, rotation/removal review later, and the exact commands `npm run secrets:inventory` and `npm run secrets:scan`.</action>
  <verify>
    <automated>npm run secrets:inventory && npm run secrets:scan</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path scripts/inventory-sensitive-files.ts -Pattern "type InventoryItem" -Quiet` returns success.
    - `Select-String -Path scripts/inventory-sensitive-files.ts -Pattern "readFileSync"` returns no matches.
    - `Select-String -Path scripts/scan-secrets.ts -Pattern "--redact" -Quiet` returns success.
    - `Select-String -Path docs/secret-hygiene.md -Pattern "Do not read, print, copy, move, delete, or commit secret values" -Quiet` returns success.
    - `npm run secrets:inventory` exits 0 and prints paths/categories only.
  </acceptance_criteria>
  <done>SETUP-07 has repeatable local inventory and redacted scanning without remediation or secret disclosure.</done>
</task>

<task type="auto">
  <name>Task 3: Replace stale README setup claims with concise links</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - docs/setup.md
    - docs/env-matrix.md
    - docs/verification.md
    - docs/secret-hygiene.md
    - prisma/schema.prisma
  </read_first>
  <action>Update `README.md` to remain concise per D-14. Replace prerequisites with Node `>=20.9.0`, npm using `package-lock.json`, Prisma MongoDB via `DATABASE_URL`, and optional external services. Replace installation steps with `npm install`, copy `.env.example` to `.env.local`, `npm run prisma:generate`, `npm run db:push`, `npm run verify:setup`, and `npm run dev`. Update tech stack database line to `Prisma ORM with MongoDB`. Add documentation links to `docs/setup.md`, `docs/env-matrix.md`, `docs/verification.md`, and `docs/secret-hygiene.md`. Do not add long-form setup details to README.</action>
  <verify>
    <automated>npm test -- readme-and-secret-hygiene --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path README.md -Pattern "Prisma MongoDB" -Quiet` returns success.
    - `Select-String -Path README.md -Pattern "docs/setup.md" -Quiet` returns success.
    - `Select-String -Path README.md -Pattern "docs/env-matrix.md" -Quiet` returns success.
    - `Select-String -Path README.md -Pattern "docs/secret-hygiene.md" -Quiet` returns success.
    - `Select-String -Path README.md -Pattern "PostgreSQL \\(Primary DB\\)|Postgres \\+ Mongo|DATABASE_URL \\(PostgreSQL\\)"` returns no matches.
  </acceptance_criteria>
  <done>README no longer sends maintainers toward PostgreSQL and links to detailed Phase 1 docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace files -> inventory output | Script enumerates sensitive-looking paths without reading contents. |
| external scanner -> terminal output | Gitleaks findings may include sensitive matches unless redacted. |
| README/docs -> maintainer behavior | Docs determine whether maintainers copy secrets or use stale database setup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | `scripts/inventory-sensitive-files.ts`, `scripts/scan-secrets.ts` | mitigate | Inventory metadata only; forbid `readFileSync`; run gitleaks with `--redact`. |
| T-1-04 | Denial of Service | `README.md` | mitigate | Remove PostgreSQL primary database claims and document Prisma MongoDB `db push`. |
| T-1-08 | Repudiation | `docs/secret-hygiene.md` | mitigate | Document that Phase 1 inventory does not remediate artifacts and rotation/removal review is a later explicit decision. |
</threat_model>

<verification>
Run `npm test -- readme-and-secret-hygiene --runInBand`, `npm run secrets:inventory`, and `npm run secrets:scan`.
</verification>

<success_criteria>
README is concise and correct, secret hygiene guidance is discoverable, and repeatable commands inventory/scan without exposing values.
</success_criteria>

<output>
After completion, create `.planning/phases/01-installable-baseline-docs-and-secret-hygiene/01-04-SUMMARY.md`.
</output>
